import type { ReactNode } from 'react';

interface ArticleBodyProps {
  badge: ReactNode;
  info: ReactNode;
  details: ReactNode;
  body: ReactNode;
  thumbnail: ReactNode;
  meta: ReactNode;
}

export type { ArticleBodyProps };
