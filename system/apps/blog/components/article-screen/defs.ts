import type { ReactNode } from 'react';

type ArticleScreenProps = {
  badge: ReactNode;
  info: ReactNode;
  details: ReactNode;
  body: ReactNode;
  thumbnail: ReactNode;
  meta: ReactNode;
}

export type { ArticleScreenProps };
