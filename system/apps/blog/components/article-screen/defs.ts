import type { ReactNode } from 'react';

interface ArticleScreenProps {
  badge: ReactNode;
  info: ReactNode;
  details: ReactNode;
  body: ReactNode;
  thumbnail: ReactNode;
  meta: ReactNode;
}

export type { ArticleScreenProps };
