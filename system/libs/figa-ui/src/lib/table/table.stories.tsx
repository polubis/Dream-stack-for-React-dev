import type { Story, Meta } from '@storybook/react';

import { Table } from '.';
import { Box } from '../box';
import { Font } from '../font';

export default {
    component: Table,
    title: 'Table'
} as Meta;
